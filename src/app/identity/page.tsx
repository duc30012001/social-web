import { ProtectedLayout } from '@/layout';
import React from 'react';

type Props = {};

function ProfilePage({}: Props) {
  return <ProtectedLayout>ProfilePage</ProtectedLayout>;
}

export default ProfilePage;
