import { ProtectedLayout } from '@/layout';
import React from 'react';

type Props = {};

function SupportPage({}: Props) {
  return <ProtectedLayout>SupportPage</ProtectedLayout>;
}

export default SupportPage;
