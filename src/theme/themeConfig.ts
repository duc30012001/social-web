import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: 'rgb(0, 82, 255)',
    fontFamily: "'Inter', sans-serif",
  },
};

export default theme;
